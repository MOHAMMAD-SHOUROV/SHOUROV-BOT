module.exports.config = {
  name: "goiadmin",
  version: "1.0.1",
  permission: 0,
  credits: "shourov",
  description: "When someone mentions the main admin, send a playful reply.",
  prefix: true,
  category: "user",
  usages: "tag admin",
  cooldowns: 5
};

module.exports.handleEvent = function({ api, event }) {
  try {
    //
    const ADMIN_ID = "100071971474157";

    //
    if (!event || !event.mentions || Object.keys(event.mentions).length === 0) return;

    //
    if (String(event.senderID) === String(ADMIN_ID)) return;

    //
    const mentionedIds = Object.keys(event.mentions).map(id => String(id));
    if (!mentionedIds.includes(String(ADMIN_ID))) return;

    // 
    const replies = [
      "Mantion_দিস না _সৌরভ বস এর মন মন ভালো নেই আস্কে-!💔🥀",
      "- আমার সাথে কেউ সেক্স করে না থুক্কু টেক্স করে নাহ🫂💔",
      "আমার একটা প্রিয়র খুব দরকার কারন আমার চোখে পানি আসার আগে নাকে সর্দি চলে আসে🤣🤣",
      "এত মেনশন না দিয়ে বক্স আসো হট করে দিবো🤷‍ঝাং 😘🥒",
      "Mantion_দিলে চুম্মাইয়া ঠুটের কালার change কইরা,লামু 💋😾😾🔨",
      "এতু ইমুশানাল কথা বলো তল দেশ দিয়ে অজরে বৃষ্টি হচ্ছে আমার 😭😭",
      "সৌরভ বস এখন বিজি। জা বলার আমাকে বলতে পারেন_!!😼🥰",
      "এতো মিনশন নাহ দিয়া সিংগেল সৌরভ রে একটা গফ দে 😒 😏",
      "Mantion_না দিয়ে সিরিয়াস প্রেম করতে চাইলে ইনবক্স",
      "মেনশন দিসনা পারলে একটা গফ দে",
      "Mantion_দিস না বাঁলপাঁক্না সৌরভ প্রচুর বিজি 🥵🥀🤐",
      "চুমু খাওয়ার বয়স টা চকলেট🍫খেয়ে উড়িয়ে দিলাম🤗"
    ];

    const chosen = replies[Math.floor(Math.random() * replies.length)];

    // 
    api.sendMessage({ body: chosen }, event.threadID, event.messageID);
  } catch (err) {
    console.error("goiadmin handleEvent error:", err);
  }
};

module.exports.run = async function({ api, event, args }) {
  // 
  return;
};